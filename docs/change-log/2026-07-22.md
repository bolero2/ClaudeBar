# 2026-07-22

## [ADD] CLAUDE.md 인수인계 문서 작성 (`/claude-md-init`)

### 내용

- 시드 CLAUDE.md의 §0(작업 원칙 RULE)을 그대로 유지하고, repo 탐색 + 사용자 인터뷰로 §1~9를 채움.
  - §1: 개인 OSS 사이드 프로젝트, 특별한 제약 없음, 외부 문서 없음 (2026-07-22 인터뷰).
  - §2~3, §7~8: `README.md`, `docs/HANDOFF.md`, `docs/change-log/`에서 확인된 사실로 구성.
  - §4: `swift build -c release` 이 세션에서 실행 확인 (증분 0.13s, 커밋 `10fd5b3`).
  - §9: macOS 26.5 / Swift 6.3 / arm64 실측.
- §0-4 보안 구체화: 실세션 스크린샷 커밋 금지 대상을 경로로 명시.

### [FIX] .gitignore 보완 — 실세션 스크린샷 유출 차단

- 기존: 루트 PNG를 개별 파일명으로 나열 → 새 디버그 스크린샷(error-1.png, error-2.png, toolbar-modal.png)이 추적 가능한 상태였음.
- 변경: 개별 나열을 `/*.png`(루트 한정)로 교체 + `docs/test/*-assets/` 추가. `docs/images/`(mock 스크린샷)는 계속 추적.
- 검증: `git check-ignore -v error-1.png error-2.png toolbar-modal.png docs/test/2026-06-15-assets/01.png` → 4건 모두 차단 확인.

### 변경 파일

- `CLAUDE.md` (전면 작성)
- `.gitignore`
- `docs/change-log/2026-07-22.md`, `docs/change-log/README.md` (본 로그)

## [ADD] 킷 하네스 편입 (`/kit-adopt`)

### 전제

- 커밋 `ff1e03d` 위에서 진행. `bootstrap.sh --adopt`가 선행 실행되어 `.gitignore`에 보안 패턴이 append되고 `.claude/skills/kit-adopt/`가 복사된 상태.
- CLAUDE.md는 같은 날 `/claude-md-init`으로 이미 시드 §0 원본 + §1~9 구조로 작성·커밋됨 → 재편 불필요, §0 충돌 규칙 없음 확인.

### 변경 사항

- `.gitignore`: adopt가 append한 범용 보안 패턴(`.env*`, `*.pem`, `id_rsa*`, `secrets/`, `.claude/settings.local.json`) 수용.
- `.claude/settings.json`: 자주 쓰는 읽기 전용 명령 allow 추가 — `git branch/ls-files/check-ignore/remote -v`, `swift build`.
- `CLAUDE.md`: §2에 `.claude/` 킷 행 추가, §5에 시크릿 패턴 행 추가, §8 현행화(미커밋 정리 TODO 완료 처리).
- `.claude/skills/kit-adopt/SKILL.md` 신규 (adopt 복사분).

### 검증 (이 세션 실측)

```bash
python3 -m json.tool .claude/settings.json          # → valid JSON (allow 추가 후 재확인)
git ls-files | grep -Ei '\.(png|env|pem|key)$|id_rsa|secrets/'
#   → docs/images/*.png 9건만 (mock 스크린샷, 허용 대상) — 추적 중인 민감 파일 없음
git check-ignore -v .env test.pem secrets/x id_rsa_test .claude/settings.local.json \
  error-1.png docs/test/2026-06-15-assets/01-sessions.png   # → 7건 모두 차단
echo '{"tool_name":"Bash","tool_input":{"command":"rm -rf /tmp/x"}}' \
  | python3 .claude/hooks/block-dangerous-bash.py           # → 차단 메시지, exit=2
```

## [UPDATE] Claude Code 현행화 — 가격표·Fable/Mythos 1M 기본 윈도우·resume 플래그

### 전제

- 커밋 `452cd37` 위에서 진행. 로컬 Claude Code `2.1.217`, macOS 26.5 / Swift 6.3 (arm64).
- 현행 모델·가격 근거: claude-api 스킬 모델 카탈로그(캐시 2026-06-24) — Fable 5 `claude-fable-5` $10/$50, Opus 4.8/4.7/4.6 $5/$25, Sonnet 5/4.6 $3/$15, Haiku 4.5 $1/$5 (모두 1M 컨텍스트, Haiku만 200K). 캐시 단가 관례: read 0.1×input, write 1.25×input.
- 로컬 실측: 최근 JSONL의 model 필드에 `claude-fable-5`(29건)·`claude-opus-4-8`(177건) 존재. `lastModelUsage` 키에 `claude-opus-4-8[1m]`은 있으나 fable의 `[1m]` variant는 없음. statusLine 캐시가 fable-5에 `context_limit=1000000` 보고.

### 발견된 갭 → 수정

| 갭 | 수정 파일 |
| --- | --- |
| 가격표가 구세대(Opus $15/$75, Haiku $0.80/$4), Fable 미지원 → fable이 sonnet 단가로 오계산 | `Core/Pricing.swift` — Fable/Mythos $10/$50 추가, Opus $5/$25(레거시 4.1↓는 $15/$75 유지), Haiku $1/$5(레거시 3.x $0.80/$4 유지) |
| 종료된 Fable 세션 윈도우가 200K로 추론(`[1m]` 마커·200K 초과에만 의존) | `Core/Models.swift` `modelDefaultsToExtended` + `inferWindow(model:)`, `Services/SessionScanner.swift` 호출부 |
| 원격 프로브도 같은 추론 사용 | `Services/RemoteScanner.swift` 인라인 파이썬 `limit` 판정에 fable/mythos 분기 |
| resume 시 `--model 'claude-fable-5[1m]'` 생성 위험(존재하지 않는 variant) | `Services/TerminalActivator.swift` `sessionFlags` — 1M 기본 모델은 bare id |
| MockData 라인업 구세대 | `MockData.swift` — claude-fable-5·claude-sonnet-5 반영 |
| SessionScanner 주석의 "2.1.x는 라이브 transcript 미기록" 서술이 2.1.217에서 반증됨 | 주석 정정 (아래 실측) |

### 실측: 2.1.217 라이브 transcript 증분 기록 (지뢰 §7 정정)

```bash
F=~/.claude/projects/-Users-nota-main-github-ClaudeDeck/<session-id>.jsonl
ls -la "$F"                                    # → 1.3MB, 활성 세션인데 존재
find ~/.claude/projects -name "*.jsonl" -mmin -3   # → 현재 활성 세션 2건의 JSONL이 잡힘
```

6월 실측("활발한 세션 2분간 0건")과 달리 2.1.217은 라이브 transcript를 증분 기록. CLAUDE.md §7에 정정 기록. statusLine 캐시 경로는 유지(구버전·버퍼링 공백·라이브 행 우선순위).

### 검증 (이 세션 실측)

```bash
swift build -c release              # Build complete! (17.70s)
.build/release/ClaudeDeck --probe   # 세션 25건 스캔
# 앵커 1: "[종료됨] nota | claude-fable-5 | ctx 0/1M" — 수정 전엔 200K로 표시될 행
# 앵커 2: "[대기] ClaudeDeck | claude-fable-5 | ctx 454.8K/1M 45%" — 라이브 캐시 경로 정상
# 앵커 3: 비용 추정 신규 단가 재계산(오늘 $155.73) — opus 중심 사용량이라 이전(구단가 $15/$75)보다 감소 방향
.build/release/ClaudeDeck --ratelimit   # five_hour/seven_day 파싱 정상(74%/45%), 미존재 키는 무시 확인
```

미검증(미측정): 원격 SSH 프로브의 fable 분기(원격 호스트에 fable 세션 없음 — 파이썬 문자열 치환만 확인), resume `--model 'claude-fable-5'` 실기기 동작.

## 이슈/논의

- `docs/HANDOFF.md`(최신화 2026-06-13)와 CLAUDE.md의 역할 중복 → 이후 CLAUDE.md로 일원화 권장.
- 미커밋 파일 정리 필요: `CLAUDE.md`, `.claude/`, `.gitignore`, `docs/test/2026-06-15.md`, 본 change-log.
