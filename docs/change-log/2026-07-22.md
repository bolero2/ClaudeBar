# 2026-07-22

## [ADD] CLAUDE.md 인수인계 문서 작성 (`/claude-md-init`)

### 내용

- 시드 CLAUDE.md의 §0(작업 원칙 RULE)을 그대로 유지하고, repo 탐색 + 사용자 인터뷰로 §1~9를 채움.
  - §1: 개인 OSS 사이드 프로젝트, 특별한 제약 없음, 외부 문서 없음 (2026-07-22 인터뷰).
  - §2~3, §7~8: `README.md`, `docs/HANDOFF.md`, `docs/change-log/`에서 확인된 사실로 구성.
  - §4: `swift build -c release` 이 세션에서 실행 확인 (증분 0.13s, 커밋 `10fd5b3`).
  - §9: macOS 26.5 / Swift 6.3 / arm64 실측.
- §0-4 보안 구체화: 실세션 스크린샷 커밋 금지 대상을 경로로 명시.

### [FIX] .gitignore 보완 — 실세션 스크린샷 유출 차단

- 기존: 루트 PNG를 개별 파일명으로 나열 → 새 디버그 스크린샷(error-1.png, error-2.png, toolbar-modal.png)이 추적 가능한 상태였음.
- 변경: 개별 나열을 `/*.png`(루트 한정)로 교체 + `docs/test/*-assets/` 추가. `docs/images/`(mock 스크린샷)는 계속 추적.
- 검증: `git check-ignore -v error-1.png error-2.png toolbar-modal.png docs/test/2026-06-15-assets/01.png` → 4건 모두 차단 확인.

### 변경 파일

- `CLAUDE.md` (전면 작성)
- `.gitignore`
- `docs/change-log/2026-07-22.md`, `docs/change-log/README.md` (본 로그)

## [ADD] 킷 하네스 편입 (`/kit-adopt`)

### 전제

- 커밋 `ff1e03d` 위에서 진행. `bootstrap.sh --adopt`가 선행 실행되어 `.gitignore`에 보안 패턴이 append되고 `.claude/skills/kit-adopt/`가 복사된 상태.
- CLAUDE.md는 같은 날 `/claude-md-init`으로 이미 시드 §0 원본 + §1~9 구조로 작성·커밋됨 → 재편 불필요, §0 충돌 규칙 없음 확인.

### 변경 사항

- `.gitignore`: adopt가 append한 범용 보안 패턴(`.env*`, `*.pem`, `id_rsa*`, `secrets/`, `.claude/settings.local.json`) 수용.
- `.claude/settings.json`: 자주 쓰는 읽기 전용 명령 allow 추가 — `git branch/ls-files/check-ignore/remote -v`, `swift build`.
- `CLAUDE.md`: §2에 `.claude/` 킷 행 추가, §5에 시크릿 패턴 행 추가, §8 현행화(미커밋 정리 TODO 완료 처리).
- `.claude/skills/kit-adopt/SKILL.md` 신규 (adopt 복사분).

### 검증 (이 세션 실측)

```bash
python3 -m json.tool .claude/settings.json          # → valid JSON (allow 추가 후 재확인)
git ls-files | grep -Ei '\.(png|env|pem|key)$|id_rsa|secrets/'
#   → docs/images/*.png 9건만 (mock 스크린샷, 허용 대상) — 추적 중인 민감 파일 없음
git check-ignore -v .env test.pem secrets/x id_rsa_test .claude/settings.local.json \
  error-1.png docs/test/2026-06-15-assets/01-sessions.png   # → 7건 모두 차단
echo '{"tool_name":"Bash","tool_input":{"command":"rm -rf /tmp/x"}}' \
  | python3 .claude/hooks/block-dangerous-bash.py           # → 차단 메시지, exit=2
```

## 이슈/논의

- `docs/HANDOFF.md`(최신화 2026-06-13)와 CLAUDE.md의 역할 중복 → 이후 CLAUDE.md로 일원화 권장.
- 미커밋 파일 정리 필요: `CLAUDE.md`, `.claude/`, `.gitignore`, `docs/test/2026-06-15.md`, 본 change-log.
